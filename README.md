

### Этот проект — **CLI‑сервис для анализа лог‑файлов**, написанный на **TypeScript** и работающий в **Node.js (ESM)**.

Сервис читает текстовый лог‑файл, парсит строки, нормализует уровень логирования и собирает агрегированную статистику (общее количество строк, количество ошибок, распределение по пользователям и т.д.).

Проект ориентирован на:
- простоту
- предсказуемость
- воспроизводимую сборку
- запуск как локально, так и в Docker

---

## 🧱 Архитектура проекта

```text
src/
  index.ts        # Точка входа (CLI)
  parser.ts       # Парсинг сырой строки лога
  normalize.ts    # Нормализация данных
  stats.ts        # Сбор статистики
  log-level.ts    # Enum уровней логирования
  log-entry.ts    # Типизированная модель лога

tests/
  parser.test.ts
  normalize.test.ts
  stats.test.ts

biome.json        # Конфигурация Biome (lint/format)
tsconfig.json     # Конфигурация TypeScript (NodeNext + ESM)
package.json
package-lock.json
Dockerfile
```

---

## ⚙️ Требования

- Node.js **20+** (рекомендуется 24)
- npm **9+**
- Docker (опционально)

---

## 🚀 Установка и запуск локально

### 1️⃣ Установка зависимостей

```bash
npm ci
```

### 2️⃣ Сборка проекта

```bash
npm run build
```

### 3️⃣ Запуск CLI

```bash
node dist/index.js <path-to-log-file>
```

Пример:

```bash
node dist/index.js ./logs.txt
```

>💡 В корне проекта есть пример файла с логами. 
---

## 🧪 Запуск тестов

Проект использует **нативный test‑runner Node.js** (`node:test`).

```bash
npm run test
```

Тесты компилируются и запускаются из `dist/tests`.

---

## 🧹 Форматирование и линтинг

Для форматирования и статического анализа используется **Biome**.

```bash
npx biome check .
```

---

## 🐳 Запуск в Docker

### 1️⃣ Сборка образа

```bash
docker build -t log-analyzer .
```

### 2️⃣ Запуск контейнера

Лог‑файл пробрасывается в контейнер через volume.

#### Linux / macOS

```bash
docker run --rm \
  -v "$(pwd)/logs.txt:/logs.txt" \
  log-analyzer /logs.txt
```

#### Windows (PowerShell)

```powershell
docker run --rm `
  -v "${PWD}\logs.txt:/logs.txt" `
  log-analyzer /logs.txt
```

---

## 🔒 Надёжность и стабильность

- Обработка ошибок чтения файлов
- Отказоустойчивый парсинг строк
- Отсутствие утечек памяти (стриминг, без накопления файлов в памяти)
- Предсказуемое поведение в CI и Docker

---

## 📎 Примечания

- Проект не использует Jest, Babel или сторонние рантаймы
- Все решения соответствуют реальному поведению Node.js

---