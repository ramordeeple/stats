/**
 * Raw-модель лог-записи.
 * Представляет данные в том виде, в котором они были распарсены из файла,
 * без валидации и строгой типизации.
 *
 * Используется только на этапе парсинга.
 */


export interface RawLogEntry {
    level?: string;
    user?: string;
}